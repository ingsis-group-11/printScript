let name : string = "John";
let age : number = 30;
let isAdult : boolean = true;
if (isAdult) {
  const bool : boolean = true;
} else {
  let bool : boolean = readInput("Hello");
  if (bool) {
    println("Hi if");
  } else {
    println("Hi else");
  }
}