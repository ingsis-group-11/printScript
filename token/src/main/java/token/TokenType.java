package token;

public enum TokenType {
  LET_KEYWORD,
  PRINT_KEYWORD,
  STRING,
  NUMBER,
  STRING_TYPE,
  NUMBER_TYPE,
  BOOLEAN_TYPE,
  IDENTIFIER,
  ASSIGN,
  SEMICOLON,
  PARENTHESIS_OPEN,
  PARENTHESIS_CLOSE,
  COLON,
  OPERATOR,
  WHITESPACE,
  READINPUT_KEYWORD,
  BOOLEAN,
  LINE_BREAK,
  IF_KEYWORD,
  ELSE_KEYWORD,
}
