package token;

public enum TokenType {
  LET_KEYWORD,
  PRINT_KEYWORD,
  STRING,
  NUMBER,
  BOOLEAN,
  STRING_TYPE,
  NUMBER_TYPE,
  BOOLEAN_TYPE,
  IDENTIFIER,
  ASSIGN,
  SEMICOLON,
  PARENTHESIS_OPEN,
  PARENTHESIS_CLOSE,
  COLON,
  OPERATOR,
  WHITESPACE,
  LINE_BREAK,
  READ_INPUT,
  IF_KEYWORD,
  ELSE_KEYWORD,
  EMPTY,
  READ_ENV, CONST_KEYWORD
}
