let bool : boolean = true;
if (bool) {
    let name : string = "John";
    let age : number = 25;
    let isAdult : boolean = true;
    if (isAdult) {
        if (false) {
            println("This is a test");
        }
    }
}