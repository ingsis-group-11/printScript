let name: string = "John";
let age: number = 30;
let isAdult: boolean = true;
if (isAdult) {
    const bool: boolean = true;
} else {
    readInput("Hello");
    if (bool) {
        readInput("Hi");
    } else {
        const sushi: string = "Sushi";
    }
}